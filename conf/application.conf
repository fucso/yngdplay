# This is the main configuration file for the application.
# https://www.playframework.com/documentation/latest/ConfigFile

# Allow access from VM
play.filters.hosts {
  allowed = ["192.168.33.10:9000"]
}


# Database settings
db.default {
  driver=com.mysql.jdbc.Driver
  url="jdbc:mysql://localhost/yngddb?characterEncoding=UTF8"
  username=dev
  password="password"
}

ebean.default = ["models.*"]